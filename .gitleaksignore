# Read docs/internals.md, "Secret scanning, and fixtures that look like secrets",
# before adding a line here. An entry repairs a fingerprint already in published
# history — one line per fingerprint, commit included. It is never the answer for
# a new fixture: change the value so it is not key-shaped instead.
#
# Two test fixtures in an earlier commit of the delivery-modules branch were
# 16-character hex strings named "laneKey", which the generic-api-key rule reads
# as a credential. They are not: they are made-up opaque handles for a fake
# module. The fixtures are changed on the branch, but the commit that carried
# them stays reachable from the ref, so the scan is told about those two
# fingerprints (commit:file:rule:line) and nothing else.
129d6da63b69817f5fdc5c7163ec3fcb0486cdc7:internal/drivers/tmux/modulelane_create_test.go:generic-api-key:141
129d6da63b69817f5fdc5c7163ec3fcb0486cdc7:internal/drivers/tmux/modulelane_create_test.go:generic-api-key:176
